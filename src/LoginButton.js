import React, {Component} from "react";
import 'bootstrap/dist/css/bootstrap.css';

export default class LoginButton extends Component {
    render() {
        return (
            <button class="btn btn-rounded" id="login-button">Login</button>
        );
    }
};